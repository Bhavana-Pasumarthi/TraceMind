# --- Database ---
POSTGRES_USER=tracemind
POSTGRES_PASSWORD=change_me_locally
POSTGRES_DB=tracemind
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg://tracemind:change_me_locally@db:5432/tracemind

# --- Backend ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:8000

# --- LLM provider (abstracted; used from Phase 9 onward) ---
# Supported: "none" (Phases 1-8 don't need a key at all), "anthropic", "openai", "local"
LLM_PROVIDER=none
LLM_MODEL=
LLM_API_KEY=

# --- Simulator ---
SIMULATOR_SEED=42
